# PDF 페이지 추출기

PDF에서 원하는 페이지만 골라 새 PDF로 만들어 줍니다.  
인쇄선(재단선)이 포함된 PDF는 자동으로 인쇄선을 제거합니다.

## 사용법

1. PDF 파일 업로드
2. 추출할 페이지 번호 입력 (예: `1, 3, 5-10`)
3. 다운로드 버튼 클릭

## 로컬 실행

```bash
pip install -r requirements.txt
streamlit run app.py
```
