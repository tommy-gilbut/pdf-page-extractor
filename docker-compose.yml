services:
  pdf-extractor:
    build: .
    container_name: pdf-extractor
    ports:
      - "8501:8501"
    restart: unless-stopped
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
